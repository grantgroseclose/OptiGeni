export const CACHE_KEY_TASKS = ['tasks'] as const;
export const CACHE_KEY_CATEGORIES = ['categories'] as const;
export const CACHE_KEY_USER = ['user'] as const;

export const TASK_ENDPOINTS = {
    TASKS: '/tasks',
    TASK: '/task'
} as const;